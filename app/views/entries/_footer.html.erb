<p class="article_footer">
    <% if current_user %>
    <!--<= Entry.status_text(entry.status) %> |-->
    <% if current_user == @entry.author %>
    <%= link_to "編集", [:edit, @entry] %> |
    <%= link_to "削除", @enrty, method: :delete, data: { confirm: "本当に削除しますか？" } %> |
    <% end %>
    <% end %>
    by <%= link_to @entry.author.name, @entry.author  %> |
    <%= @entry.posted_at.strftime("%Y/%m/%d %H:%M") %>
</p>